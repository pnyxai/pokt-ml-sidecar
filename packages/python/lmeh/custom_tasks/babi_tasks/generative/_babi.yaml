group: babi_tasks
group_alias: babi (generative)
task:
  - group: babi-all
    task:
      - babi-all
    aggregate_metric_list:
    - aggregation: mean
      metric: exact_match
      weight_by_size: true
      filter_list: get_response
aggregate_metric_list:
  - aggregation: mean
    metric: exact_match
    weight_by_size: true
    filter_list: get_response
metadata:
  version: 1